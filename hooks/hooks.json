{
  "description": ".block enforcement - blocks file modifications in protected directories",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Edit|Write|NotebookEdit|Bash",
        "hooks": [
          {
            "type": "command",
            "command": "${CLAUDE_PLUGIN_ROOT}/hooks/protect-directories.sh",
            "timeout": 5000
          }
        ]
      }
    ]
  }
}
